# DBLens – copy to .env and set values (VM / Docker)
# Used by: docker compose up

# PostgreSQL (used by the app container to connect to postgres service)
DATABASE_USER=dblens
DATABASE_PASSWORD=dblens_secret
DATABASE_NAME=dblens

# Required in production (change these!)
JWT_SECRET_KEY=change-me-use-a-long-random-string-at-least-32-chars
# Generate: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=

# Optional: if you use an external PostgreSQL, point app to it and remove postgres service from compose
# DATABASE_HOST=postgres
# DATABASE_PORT=5432
